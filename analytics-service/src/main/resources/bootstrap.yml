spring:
  application:
    name: analytics-service
  profiles:
    active: analytics
  cloud:
    config:
      name: analytics-service,config-client
      uri: http://localhost:8888
      username: user
      password: user